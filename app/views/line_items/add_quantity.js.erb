

var quantity = document.getElementById("#Q")
<%# console.log(typeof quantity.innerHTML); %>

num = 0
num = + quantity.innerHTML
num += 1
console.log(typeof num, num, "this is num")
<%# console.log(quantity) %>

str_num = num.toString();

console.log("str_num", str_num)

$('.add_Q').bind('ajax:success', function(evt, data, status, xhr) {
    evt.preventDefault();
    document.getElementById("Q").innerHTML = str_num;
    $(this).closest(".Q").innerHTML = str_num;
    console.log(document.getElementById("Q").innerHTML)
  })

