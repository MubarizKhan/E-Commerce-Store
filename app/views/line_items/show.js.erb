<%# $(".merge_button").click(function(event){
    event.preventDefault();
    var searchIDs = $(".table input:checkbox:checked").map(function(){
      return $(this).val();
    }).get(); // <----
    console.log(searchIDs);
}); %>

console.log("hiitttt");
<%# document.getElementsByClassName("CBT")[2].innerHTML %>

$('.CBT').bind('ajax:success', function() {
	<%# $('tr').remove(); %>
  event.preventDefault();
    var searchIDs = $(".table input:checkbox:checked").map(function(){
      return $(this).val();
    }).get(); // <----
    console.log(searchIDs);
});
